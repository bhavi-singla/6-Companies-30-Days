Minimum Size Subarray Sum
Solution:-
int minSubArrayLen(int target, vector<int>& nums) {
        int sum=0;
        int len=INT_MAX;
        int start=0;
        int end=0;
        while(end<nums.size())
        {
            sum+=nums[end];
            end++;
            while(sum>=target)
            {
                len=min(len,end-start);
                sum=sum-nums[start];
                start++;
            }
        }
        if(len==INT_MAX)
            return 0;
        return len;
    }
